[SERVICE]
    parsers_file /tmp/parsers.conf

[INPUT]
    name                tail
    path                /home/fluentbit/*.log
    parser              nginx
    refresh_interval    1
    read_from_head      true

[OUTPUT]
    name    stdout
    match   *
